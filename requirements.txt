fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.15.0
scipy==1.14.1
numpy==2.1.1
ib_insync==0.9.86
python-dotenv==1.0.1
anthropic==0.39.0
PyYAML==6.0.2

# Step 22: validation-cohort reporting/export layer.
openpyxl==3.1.5
reportlab==5.0.1

# Step 22.1: Alpaca market-data-ONLY provider (src/data/alpaca_provider.py,
# src/data/alpaca_historical.py). Only alpaca.data.* (read-only market-data
# clients) is ever imported -- see those modules' own docstrings and
# tests/acceptance/test_alpaca_market_data_only.py for the enforced
# guarantee that alpaca.trading (the order-submission client) never is.
alpaca-py==0.44.0
